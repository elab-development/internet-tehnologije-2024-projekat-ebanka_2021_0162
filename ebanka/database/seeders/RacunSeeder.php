<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class RacunSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */

    /*protected $user;
    protected $banka;
    protected $tekuci;
    protected $studentski;
    protected $stedni;
    protected $devizni;*/


    /*public function __construct($user1, $banka1, $tekuci1, $stedni1, $devizni1, $studentski1){
        $this->user=$user1;
        $this->banka=$banka1;    
        $this->tekuci=$tekuci1;
        $this->studentski=$studentski1;
        $this->stedni=$stedni1;
        $this->devizni=$devizni1;
    }*/



    public function run()
    {
       /* 
        if(!is_null($tekuci)){
            Racun::factory()->create([
                'user_id'=>$user->id,
                'banka_id'=>$banka->id,
                'racunTip_type'=>$tekuci,
                'racunTip_id'=>$tekuci->id,
            ]);
        }

        if(!is_null($stedni)){
            Racun::factory()->create([
                'user_id'=>$user->id,
                'banka_id'=>$banka->id,
                'racunTip_type'=>$stedni,
                'racunTip_id'=>$stedni->id,
            ]);
        }

        if(!is_null($devizni)){
            Racun::factory()->create([
                'user_id'=>$user->id,
                'banka_id'=>$banka->id,
                'racunTip_type'=>$devizni,
                'racunTip_id'=>$devizni->id,
            ]);
        }


        if(!is_null($studentski)){
            Racun::factory()->create([
                'user_id'=>$user->id,
                'banka_id'=>$banka->id,
                'racunTip_type'=>$studentski,
                'racunTip_id'=>$studentski->id,
            ]);
        }*/

        
    }
}
